name: 'test45'
author: 'Kevin Li'
description: 'test lib'

inputs:
  registry:
    description: 'docker registry'
    required: true
    default: 'containerregistrydev.azurecr.io'
  image-name:
    description: 'docker image name'
    required: true
    default: 'webjet-github'

runs:
  using: "composite"
  steps: 
    - run: |
        echo "12345678"
        GITHUBSHA=${{ github.sha }}
        TAG=$(echo $GITHUBSHA | cut -c1-6)
        echo $TAG
        echo ${{ github.sha }}
        echo "TAG done"
        chmod +x $GITHUB_ACTION_PATH/test.sh
        $GITHUB_ACTION_PATH/test.sh ${{ inputs.registry }} ${{ inputs.image-name }}
      shell: bash
