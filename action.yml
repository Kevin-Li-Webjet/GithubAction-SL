name: 'test'
author: 'Kevin Li'
description: 'test lib'

inputs:
  v1:
    description: 'test v1'
    required: true
    default: 'dev'

runs:
  using: "composite"
  steps: 
    - run: $GITHUB_ACTION_PATH/test.sh
      shell: bash
