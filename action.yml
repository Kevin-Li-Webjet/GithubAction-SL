name: 'test45'
author: 'Kevin Li'
description: 'test lib'

inputs:
  v1:
    description: 'test v1'
    required: true
    default: 'dev'

runs:
  using: "composite"
  steps: 
    - run: |
        echo "1234567"
        echo ${{ inputs.v1 }}
        chmod +x $GITHUB_ACTION_PATH/script.sh
        chmod +x $GITHUB_ACTION_PATH/test.sh
        $GITHUB_ACTION_PATH/test.sh ${{ inputs.v1 }}
      shell: bash
    - run: $GITHUB_ACTION_PATH/script.sh 
      shell: bash